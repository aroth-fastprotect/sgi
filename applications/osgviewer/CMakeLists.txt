include_directories(../viewer_base)

SET( TARGET_SRC
    osgviewer.cpp
)

SET(TARGET_LIBRARIES_VARS
    OPENTHREADS_LIBRARY
    OSG_LIBRARY
    OSGDB_LIBRARY
    OSGUTIL_LIBRARY
    OSGGA_LIBRARY
    OSGVIEWER_LIBRARY
    sgi_viewer_base
)
LIST(APPEND TARGET_LIBRARIES_VARS
    QT_QTCORE_LIBRARY
    QT_QTGUI_LIBRARY
    )
IF ( Qt5Widgets_FOUND )
    # Add the include directories for the Qt 5 Widgets module to
    # the compile lines.
    include_directories(${Qt5Widgets_INCLUDE_DIRS})
	LIST(APPEND TARGET_LIBRARIES_VARS QT_QTWIDGETS_LIBRARY )
ENDIF()
IF(OSGEARTH_FOUND)
    LIST(APPEND TARGET_LIBRARIES_VARS
        OSGEARTH_LIBRARY
        OSGEARTHUTIL_LIBRARY
    )
ENDIF()
if(OSGQT_FOUND)
    include_directories(${OSGQT_INCLUDE_DIR})
    LIST(APPEND TARGET_LIBRARIES_VARS
        OSGQT_LIBRARY
    )
endif()

FAST_SET_TARGET_AND_PRODUCT_VERSION(SGI)
SET(TARGET_NO_TRANSLATION 1)

FAST_COMMANDLINE_APPLICATION( sgi_osgviewer NO_MOC_INCLUDE)
