LIST(APPEND TARGET_SRC
        main.cpp
        )

LIST(APPEND TARGET_MOC_H
    main.h
    )

LIST(APPEND TARGET_QRC
        sgi_logo.qrc
        )

#OTHER_FILES += sgi_loader.json
LIST(APPEND TARGET_LIBRARIES_VARS
    QT_QTCORE_LIBRARY
    QT_QTGUI_LIBRARY
    )
IF ( Qt5Widgets_FOUND )
    LIST(APPEND TARGET_LIBRARIES_VARS QT_QTWIDGETS_LIBRARY )
ENDIF()

include_directories(${QT_INCLUDE_DIR}/QtWidgets/${QT5_VERSION} ${QT_INCLUDE_DIR}/QtGui/${QT5_VERSION} ${QT_INCLUDE_DIR}/QtCore/${QT5_VERSION})

FAST_PLUGIN(qsgi_loader PLUGINTYPE imageformats )
